import {
  //action
} from './../actions'

let default_state = {
  example: true
}


const exampleReducer = (state = default_state, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default exampleReducer